name: Cost Start

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: ap-northeast-1
  TF_WORKING_DIR: envs/dev
  ASG_NAME: tomario-dev-asg
  RDS_INSTANCE: tomario-dev-rds

jobs:
  cost-start:
    name: Start Resources
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ${{ env.TF_WORKING_DIR }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Start RDS
        run: |
          aws rds start-db-instance \
            --db-instance-identifier ${{ env.RDS_INSTANCE }}

      - name: Wait for RDS to be available
        run: |
          aws rds wait db-instance-available \
            --db-instance-identifier ${{ env.RDS_INSTANCE }}

      - name: Start EC2 (ASG min/desired = 1)
        run: |
          aws autoscaling update-auto-scaling-group \
            --auto-scaling-group-name ${{ env.ASG_NAME }} \
            --min-size 1 \
            --desired-capacity 1

      - name: Terraform Init
        run: terraform init

      - name: Create ALB (terraform apply -target=module.backend)
        run: terraform apply -target=module.backend -auto-approve
